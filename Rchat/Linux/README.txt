To run the script run these commands in the termanil.

1) Goto this file location 
2) chmod +x JoinPrivateChat.sh
3) sudo ./JoinPrivateChat.sh

Must run the script with sudo because it needs to install some
programs to make the script work. (like ncat)